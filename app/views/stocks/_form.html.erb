<%= form_with(model: stock, local: true) do |form| %>
  <% if stock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stock.errors.count, "error") %> prohibited this stock from being saved:</h2>

      <ul>
      <% stock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :company %>
    <%= form.text_field :company, id: :stock_company %>
  </div>

  <div class="field">
    <%= form.label :ticker %>
    <%= form.text_field :ticker, id: :stock_ticker %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :stock_price %>
  </div>

  <div class="field">
    <%= form.label :dividend %>
    <%= form.text_field :dividend, id: :stock_dividend %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.check_box :type, id: :stock_type %>
  </div>

  <div class="field">
    <%= form.label :class %>
    <%= form.text_field :class, id: :stock_class %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
