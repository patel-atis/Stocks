<p id="notice"><%= notice %></p>

<div id="post-list">
  <h1>Posts</h1>
  <ul>
    <% @posts.each do |post| %>
      <td><%= post.stock_id %></td>
      <td><%= post.user_id %></td>
      <td><%= post.content %></td>
    <% end %>
  </ul>
</div>
